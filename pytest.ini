[pytest]
addopts = -v --tb=short --maxfail=10
testpaths = tests
python_files = test_*.py
markers =
    smoke: smoke tests
    regression: regression tests
    slow: slow tests
    prod_only: tests that should only run in production environments
log_cli = true
log_cli_level = INFO